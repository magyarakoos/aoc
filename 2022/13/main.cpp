#include <bits/stdc++.h>
using namespace std;

union Token {
    vector<Token> list;
    int x;
};

vector<Token> tokenize(string s) {
    vector<Token> v;
    if ()
}

int main() {
    ifstream f("input");
    string s, t;
    int p1 = 0;
    while (f >> s >> t) {
        
    }
}