#include <bits/stdc++.h>
using namespace std;
using ll = unsigned long long;

ll solve(const string& s, const vector<int>& groupS) {

}

int main() {
    ifstream f("input");
    string s, t;
    while (f >> s >> t) {
        replace(t.begin(), t.end(), ',', ' ');
        stringstream ss(t);

    }
}