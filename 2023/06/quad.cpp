#include <bits/stdc++.h>
using namespace std;

int main() {
    ifstream f("input");
    string s;
    f >> s;
    vector<int> t({0}), d({0});
    while (f >> s && s[0] != '') {
        t.push_back(0);
    }
}
