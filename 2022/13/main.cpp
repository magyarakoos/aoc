#include <bits/stdc++.h>
using namespace std;

int main() {
    ifstream f("input");
    string s, t;
    while (f >> s >> t) {
        for (char& c : s) {
            if (c == '[' || c == ']' || c == ',') {
                c = 
            }
        }
    }
}