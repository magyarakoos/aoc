#include <bits/stdc++.h>
using namespace std;

int main() {
    ifstream f("input");
    string s, t;
    int p1 = 0;
    while (f >> s >> t) {
        vector<int> sv = tokenize(s), tv = tokenize(t);
        if (sv.size() < tv.size()) {
            for (int i = 0; i < sv.size(); i++) {
                if (sv)
            }
            p1++;
        }
    }
}