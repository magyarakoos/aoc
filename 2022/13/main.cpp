#include <bits/stdc++.h>
using namespace std;

struct token {
    union {
        vector<token> list;
        int x;
    };
}

int main() {
    ifstream f("input");
    string s, t;
    int p1 = 0;
    while (f >> s >> t) {
        
    }
}