#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<vector<bool>> v({});
    ifstream f("input");
    string s;
    while (f >> s) {
        for (char c :s) {
            v.back().push_back()
        }
    }
}